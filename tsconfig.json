{
  "compilerOptions": {
    "module": "commonjs",
    "target": "ES2015", // ES버전
    "sourceMap": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*"], // 컴파일과정 | 배열로 적용 // **/* src안에 있는 파일
  "exclude": ["node_modules"]
}

// 항상 프로젝트 사용하는 셋업
